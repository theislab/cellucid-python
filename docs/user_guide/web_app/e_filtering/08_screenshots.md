# Verified filtering capture

```{figure} ../../../_static/screenshots/filtering/coloring-filtering-cell-type-panel.png
:alt: Coloring and Filtering panel with a categorical cell-type field selected and the unfiltered state reported.
:width: 246px

The panel reports the active categorical field and the current visibility state.
Here no filter is active, so all points remain visible.
```
