# API Reference Coverage

`cellucid-r` intentionally exposes a very small public API:

- `cellucid_prepare()`
- `prepare()` (alias)

This section maps:
- what is exported,
- where each argument is documented in this user guide,
- and how to interpret common errors.

```{toctree}
:maxdepth: 1
:hidden:
:glob:

[0-9]*
```
